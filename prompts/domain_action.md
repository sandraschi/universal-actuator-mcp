# Universal Domain Orchestration Template

Use this template to orchestrate complex actions across multiple domains using the Universal Actuator.

## Workflow Goal
[Describe the objective, e.g., "Analyze frontend codebase and perform UI tweaks"]

## Execution Sequence

### 1. Exploration (Files Domain)
Execute `universal_actuator` to discover relevant files:
```json
{
  "domain": "files",
  "action": "list_directory",
  "payload": { "path": "[target_path]", "recursive": true }
}
```

### 2. Analysis (Knowledge Domain)
Consult the Advanced Memory to see if this area has been researched:
```json
{
  "domain": "knowledge",
  "action": "adn_knowledge",
  "payload": { "operation": "search", "query": "[topic]" }
}
```

### 3. Implementation (Files/Generative Domain)
Perform the actual work:
```json
{
  "domain": "files",
  "action": "write_file",
  "payload": { "path": "[file_path]", "content": "[new_content]" }
}
```

### 4. Verification (System Domain)
Check system status or trigger verification scripts:
```json
{
  "domain": "system",
  "action": "info",
  "payload": {}
}
```

---
Generated by Universal Actuator SOTA Pattern
